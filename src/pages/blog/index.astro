---
import Acomodo from '../../layouts/Acomodo.astro';
import { Image } from 'astro:assets';
import myImage from "../../imagenes/cimarron.webp"; 
const posts = await Astro.glob('./posts/*.md');

---


<Acomodo tituloPage='Blog de desarrollo' descipcionPage='Vamos a ir poniendo la estrategia y el programa'>
  <h1>blog</h1> 
  <Image src={myImage} alt="Una descripción de mi imagen." width="300"/>

  
  {
    posts.map(post => {
      return   <a href={post.url} class="link">{post.frontmatter.title}</a>

    })
  }
</Acomodo>

<style>
  .link {
    background: wheat;
    color: black;
    text-decoration: none;
    display: block;
    margin-bottom: 4px;
  }
</style>